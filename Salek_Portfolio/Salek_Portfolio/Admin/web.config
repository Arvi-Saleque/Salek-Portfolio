<?xml version="1.0"?>
<configuration>
  <system.web>
    <!-- Only authenticated users may access anything under /Admin -->
    <authorization>
      <deny users="?" />
    </authorization>
    <pages validateRequest="true" />
  </system.web>
</configuration>
